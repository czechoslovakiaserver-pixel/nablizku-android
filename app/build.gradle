plugins {
    id "com.android.application"
}

android {
    namespace "cz.nablizku.app"
    compileSdk 35

    defaultConfig {
        applicationId "cz.nablizku.app"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }
}
