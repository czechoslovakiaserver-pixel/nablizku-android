plugins {
    id "com.android.application"
}

android {
    namespace "cz.nablizku.app"
    compileSdk 35

    defaultConfig {
        applicationId "cz.nablizku.app"
        minSdk 26
        targetSdk 35
        versionCode 3
        versionName "0.2.0"
    }
}
